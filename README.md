# Ellie's AI-generated data tracking app

## Overview
A better journaling and data tracking app.

## Goals
- I want a way to view data in a web app
- I want to create a script to migrate all data in my `data-app` directory
- I want to create a script to migrate all data in my `notes` directory
- I want to hook the data up to an LLM and make it a chatbot
